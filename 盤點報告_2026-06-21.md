# travel-shopping-list 盤點報告
**日期**：2026-06-21 深夜（台北時間）
**盤點範圍**：/Users/andyc/Desktop/agent/travel-shopping-list

---

## 快速摘要
- 🔴 高嚴重度：5 項
- 🟡 中嚴重度：5 項
- 🟢 低/衛生：4 項
- 直接修了：無（0 項）——所有問題均需 Andy 決策或涉及 worker/金鑰

---

## 1. BUG（實際與潛在）

### 🔴 B-1：`fetchCandidates` 未檢查 HTTP 狀態碼
**位置**：`App.js` 第 235–237 行
```js
const res = await fetch(`${WORKER_URL}/price?...`);
const data = await res.json();
if (!data.ok) throw new Error(...)
```
Worker 回 4xx/5xx 時，`res.json()` 可能失敗直接 throw，但錯誤訊息是 "SyntaxError: JSON Parse error"，沒有清楚提示用戶是網路問題。

**怎麼修**：加 `if (!res.ok) throw new Error(...${res.status}...)` 在 `.json()` 前。

---

### 🟡 B-2：`todoCards` useMemo 依賴包含 `renderTodo`（閉包），但未列入 deps
**位置**：`App.js` 第 704 行
```js
const todoCards = useMemo(() => todoItems.map(renderTodo), [todoItems, busy, expanded, rates]);
```
`renderTodo` 是每次 render 重新定義的閉包函式，但並未加入 deps。`priceEdit`、`storeEdit`、`spotLinks` 等狀態改變時，`renderTodo` 的閉包捕捉到新值但 `useMemo` 不會重新計算，可能顯示過時 UI。

**怎麼修**：把 `renderTodo` 用 `useCallback` 包起來，並正確列出所有 deps；或直接移除 `useMemo`（列表通常在百項以內，差異不大）。

---

### 🟡 B-3：`effLocal` 在 `todoTotal` 的 useMemo 裡被呼叫，但 `effLocal` 本身依賴 `rates`，deps 已有 `rates`，這點 OK。但 `effLocal` 計算 `range` 時用的是 `(min + max) / 2`，未考慮空 range 物件（`{min:0, max:0}`），會把 0 價格當成有效均價。
**位置**：`App.js` 第 571 行

**怎麼修**：加 `if (it.range && it.range.min === 0 && it.range.max === 0) return 0` 的保護，或查價時就濾掉 price=0 的候選（前端的 `cands.filter(c => c.price > 0)` 其實已有，但 range 計算沒連動）。

---

### 🟡 B-4：`openMore` 日本路徑沒有傳入 `item.currency`
**位置**：`App.js` 第 450–455 行
```js
setPicker({ itemId: item.id, currency: item.currency || CO[item.country].cur, ... })
```
若 `item.currency` 已被設成 `¥` 以外的值，`openMore` 回傳的 picker 可能用錯幣值。邊界問題，機率低。

---

### 🟢 B-5：`scanImage` base64 失敗無提示
**位置**：`App.js` 第 484 行
```js
if (!b64) { setScanning(false); return; }
```
靜默退出，使用者不知道為什麼沒反應。

**怎麼修**：加 `alert('無法取得圖片資料，請重試')` 後再 return。

---

## 2. 安全性（金鑰/外洩/版控）

### 🔴 S-1：樂天 `applicationId` 與 `accessKey` 寫死在前端，進入版控 + 打入 bundle
**位置**：`App.js` 第 17–18 行
```js
const RAKUTEN_APP_ID = '3ec6041d-d772-4a05-861a-9d15ec64dafa';
const RAKUTEN_ACCESS_KEY = 'pk_SuWs3ZCwNcFZS14BLH8QbOcDkOOBMKlyDRp7L3a2ANN';
```
已確認在 `dist/_expo/static/js/web/index-*.js` bundle 可直接搜尋到，且所有 git commit history 均有紀錄（無法靠 .gitignore 補救，歷史永久存在）。

雖然 worker/src/index.js 的 comment 說 `pk_` 是「可公開的 publishable key，綁網域放後端 OK」，但：
1. 樂天的 `pk_` accessKey 是否真的做了網域限制，目前沒有驗證過
2. `applicationId` 是 app 識別碼，不應公開
3. 前端直接呼叫樂天 API 會讓任何人可以用你的 appId 刷免費額度

**怎麼修**：
- 短期：登入樂天開發者後台確認 accessKey 的「allowed domains」設定，確保只允許你的 GitHub Pages 網域
- 長期：把日本樂天搜尋也移到 worker 後端代理（worker 帶 Referer 即可），前端不要直連；金鑰存 Cloudflare secret，不要放程式碼

---

### 🔴 S-2：CORS `Access-Control-Allow-Origin: *`（Worker）
**位置**：`worker/src/index.js` 第 35–39 行
Worker 所有端點（包括 `/vision`、`/translate`、`/popular`、`/price`）全部允許任意 origin 存取。任何人都可以直接呼叫 `/vision` 傳 base64 圖片消耗你的 OpenAI 額度，或呼叫 `/translate` 批次翻譯。

**怎麼修（建議，不要動 worker 本體）**：記錄為待辦，由 Andy 決定改法：
- 可考慮在 worker 中限定允許的 Origin（僅你的 GitHub Pages、localhost）
- 或在敏感端點（/vision、/translate）加 secret header 驗證

---

### 🔴 S-3：Worker 無 Rate Limiting（已知問題）
Worker 的 `/vision`（OpenAI 視覺 API）、`/translate`、`/price`（Naver/GPT）均無任何呼叫頻率限制，任何人可以無限刷，直接燒掉你的 OpenAI + Naver API 額度。

**怎麼修（建議，不要動 worker）**：
- 加入 Cloudflare Workers Rate Limiting（CF 原生支援，按 IP 限流）
- 或在 Worker 前加 CF WAF 規則

---

### 🟡 S-4：Worker `/admin` 端點完全公開
**位置**：`worker/src/index.js` 第 894–895 行
```js
if (url.pathname === '/admin') {
  return new Response(ADMIN_HTML, ...)
```
管理後台 HTML 無任何存取限制（但登入需密碼），任何人都能下載整個 ADMIN_HTML（含 Alpine.js 邏輯）。雖然 token 驗證在後端，但暴露後台存在、完整 UI 程式碼是安全慣例問題。

**怎麼修**：可加一個基本的 Referer 或自訂 header 篩選，或把 `/admin` 路徑改成 `/admin-<隨機字串>` 增加安全模糊度。

---

### 🟡 S-5：`.gitignore` 沒有排除 `dist/`，build 產物（含金鑰 bundle）已進版控
**位置**：`.gitignore` 第 2 行已有 `dist/`——等等，確認一下：
```
/node_modules/
dist/
```
`.gitignore` 有 `dist/`，但 `git status` 沒顯示 dist/ 為 untracked，代表 dist/ 可能之前已被 commit 進去（先加進去再加 gitignore 不會自動移除追蹤）。

**確認方式**：`git ls-files dist/ | head -5`——若有輸出代表已追蹤。

**怎麼修**：`git rm -r --cached dist/` 然後 commit，才會真正從版本控制移除（但歷史仍有）。

---

## 3. 成本/風控

### 🔴 C-1：Worker 無 Rate Limiting（見 S-3）

### 🔴 C-2：`/vision` 端點接受任意大小 base64 圖片，無大小限制
**位置**：`worker/src/index.js` 第 979 行
```js
const b64 = (body.image || '').replace(/^data:image\/\w+;base64,/, '');
```
沒有限制圖片大小，攻擊者可傳超大 base64 字串，造成 Worker 記憶體壓力（CF Workers 有 128MB 記憶體限制），且每次都會帶進 OpenAI API 消耗 token。

**怎麼修（建議）**：加 `if (b64.length > 500_000) return json({ok:false, error:'image too large'}, 413)` 保護（約 375KB 原始圖，app 端已壓 0.5 quality）。

---

### 🟡 C-3：`queryAll` 沒有並發控制，大量商品時連續打 API
**位置**：`App.js` 第 431–434 行
```js
for (const it of todo) { await queryOne(it); }
```
雖然是序列（await），但若 todo 清單有 20 筆，會依序打 20 次 worker → 20 次 GPT/Naver，可能讓 Naver API 觸發限流（Naver 每秒 10 req 限制）。

**怎麼修**：加延遲（每次 sleep 200ms），或限制一次最多查 10 筆。

---

## 4. 多餘程式碼

### 🟢 D-1：`worker/src/index.js` 有未 commit 的 dirty 變更（/directions + /staticmap 多點路線），但 App 前端完全未使用這些端點
Git diff 顯示 worker 有新增 `/directions` 端點（Google Routes API）及 `/staticmap` 多點功能，但前端 App.js 沒有呼叫這些端點的程式碼——這是 travel-planner 的遺留功能被加入這個 worker（因為共用同一個 `menshui-price` worker）。

**怎麼修**：這些功能本身無害（只是多餘），但要注意它會增加 worker 的攻擊面（GOOGLE_MAPS_KEY 可被任意呼叫 `/directions`）。建議向 Andy 確認是否保留或移除。

---

### 🟢 D-2：`App.js` 有未使用的 style 定義
如 `priceRow`、`flex1`、`heroRate`、`loadingTxt`、`noPrice` 等樣式已定義但程式碼中搜尋不到使用（已被更新的 UI 取代）。

**怎麼修**：不影響功能，可用 eslint-plugin-react-native 檢查後清理。

---

## 5. 邏輯衝突

### 🟡 L-1：`switchCountry` 清除商品資料邏輯，但「依購物點」檢視 `todoSpotGroups` 用的是 `todoItems`（已 sort 過）的 `it.country`
當全域切換國家時，`switchCountry` 會更新所有 `status==='todo'` 的商品 country。但若使用者手動切了某筆商品的 country（miniSeg），然後全域切換，那筆商品的 country 也會被覆蓋——這可能不是預期行為（使用者明確設定個別商品的國家應該被保留）。

**怎麼修**：加一個 `countryManual` 標記（類似 `imageManual`），手動設過 country 的商品就不被全域 switchCountry 覆蓋。

---

### 🟡 L-2：`stores`（常用店家記憶）最多保留 8 個，但 `spotLinks` 沒有對應清理
**位置**：`App.js` 第 383 行
```js
setStores(prev => [n, ...prev].slice(0, 8));
```
`stores` 陣列最多 8 個，但 `spotLinks` 物件無上限，舊的地點資料永遠留著，理論上 AsyncStorage 資料會無限增長（但實際影響極小）。

**怎麼修**：在 `setStores` 時，同步清除 `spotLinks` 中不在新 `stores` 裡的 key。

---

## 6. 版面配置

### 🟢 V-1：PWA 模式下 iOS Safari 底部安全區域
這是 Expo web/PWA 的常見問題：若使用者把 app 加到主畫面（PWA），iOS Safari 的 home indicator 可能遮住底部 Toast 和按鈕。
`styles.toastWrap` 有 `bottom: 28`，目前 hardcode，不是 `env(safe-area-inset-bottom)`。

**怎麼修**：在 `expo-status-bar` 已啟用的情況下，可用 `react-native-safe-area-context` 的 `useSafeAreaInsets()` 取得底部安全距離動態加入。

---

### 🟢 V-2：`storeModal` 的 `maxHeight` 固定為數字（460、540、560），小螢幕可能還是會被截斷
**位置**：`App.js` 各 Modal 內的 ScrollView `maxHeight`

**怎麼修**：改用 `Math.min(460, screenHeight * 0.65)` 動態限高。

---

## 7. 相依套件風險

### 🟡 NPM-1：10 個 moderate 漏洞（uuid < 11.1.1）
```
uuid < 11.1.1 — Missing buffer bounds check in v3/v5/v6 (GHSA-w5hq-g745-h8pq)
  → xcode → @expo/config-plugins → @expo/cli → expo
```
整條鏈都是 expo 內部依賴，修法是降級到 expo@46（breaking change），不建議強制修。

**怎麼修**：
- 先確認 uuid 的 v3/v5/v6 函式在 expo 內部的使用是否帶了 `buf` 參數（若沒有就是理論漏洞，實際低風險）
- 等 expo@57 或更新版本官方修復
- 目前不建議 `npm audit fix --force`（會 downgrade expo 到 v46）

---

## 8. 建置與上線

### 🟡 BUILD-1：`dist/` 可能已被 git 追蹤（含 bundle 內的 Rakuten 金鑰）
見 S-5。已有 bundle 進版控等於金鑰已公開在 GitHub repo 歷史中（即使後來 .gitignore 也無法從歷史移除）。

**怎麼修**：
1. 確認 `git ls-files dist/ | head` 是否有輸出
2. 若有，`git rm -r --cached dist/` 後 commit（不影響本地 dist/）
3. 若 GitHub 上 repo 是 public，需考慮輪換樂天金鑰

---

### 🟢 BUILD-2：`package.json` 無 build script
沒有 `"build": "expo export -p web"` 的 script，每次重建要手動記指令。

**怎麼修**：加一行 `"build": "expo export -p web --output-dir dist"` 到 `scripts`。

---

## 9. Git 衛生

### 🟡 GIT-1：`worker/src/index.js` 有未 commit 的變更（dirty）
`git status` 顯示 worker 有修改但未 stage/commit。變更內容是 `/directions` 路由與 `/staticmap` 多點功能（非必要的 travel-planner 遺留）。

**怎麼修**：請 Andy 決定：(a) 若這些是要保留的功能 → commit；(b) 若不需要 → `git checkout -- worker/src/index.js` 還原（注意：不要部署 worker）。

---

### 🟢 GIT-2：Rakuten 金鑰在 git history 永久存在
即使後來改用環境變數，歷史中的 commit 仍可看到明文金鑰（`git log --all -p` 可查）。若 repo 是 public，任何人都能撈到歷史。

**怎麼修**：若安全要求高，用 `git filter-repo` 清除歷史（高風險，需評估必要性）；或直接向樂天申請換一組金鑰。

---

## 10. 優化建議

### 🟡 OPT-1：樂天搜尋直接從前端打，會暴露 Referer（使用者瀏覽器網址）給樂天
**位置**：`App.js` 第 200–231 行
前端直連樂天 API，HTTP Referer 是使用者當前頁面網址，樂天可收集流量分析。

**建議**：日本樂天搜尋移到 worker 後端代理（worker comment 說「因 Worker 帶不了 Referer」但實際測試是 OK 的，worker/src/index.js 第 19 行已有帶 Referer 的做法）。

---

### 🟢 OPT-2：`queryAll` 沒有進度回饋
批次查價時只有個別卡片 loading spinner，沒有全局進度（例如「查 3/7」），使用者不知道還要等多久。

---

### 🟢 OPT-3：`AsyncStorage` 資料無版本遷移機制
若未來欄位結構改變，舊版本資料可能讀取異常（`STORE_KEY = 'travel-shopping-list-v1'` 已有版本標記，但沒有 migration 邏輯）。

---

## 盤點人直接修了什麼

**無**。所有問題均需 Andy 決策，或涉及 worker 相關操作（依指示不碰）、或 git 操作（需確認後再執行）。

---

## 需 Andy 決策的事項清單

| 優先 | 事項 | 說明 |
|------|------|------|
| 🔴 高 | 樂天金鑰暴露 | 確認 pk_ 是否有網域白名單；評估是否要移到 worker |
| 🔴 高 | Worker 無限流（已知） | 決定是否在 CF Dashboard 加 Rate Limit rule |
| 🔴 高 | Worker CORS=* | 決定是否限縮允許 origin |
| 🟡 中 | dist/ 是否已在 git 版控 | `git ls-files dist/` 確認，若是要執行 git rm --cached |
| 🟡 中 | worker dirty 變更 | 決定保留（commit）或還原 |
| 🟡 中 | uuid 漏洞 | 確認影響範圍，等 expo 官方更新 |
